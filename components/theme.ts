import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    text: {
      primary: "#1A1A28",
    },
    common: {
      black: "#1A1A28",
    },
  },
});

export default theme;
