import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: { main: "#000077" },
    text: {
      primary: "#1A1A28",
    },
    common: {
      black: "#1A1A28",
    },
  },
});

export default theme;
